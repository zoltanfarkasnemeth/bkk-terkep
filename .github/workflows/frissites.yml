name: BKK Adat Frissites
on:
  schedule:
    - cron: '*/30 * * * *' # 30 percenként frissül
  workflow_dispatch:      # Kézzel is elindíthatod az Actions fülön

jobs:
  download-and-save:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout kód
        uses: actions/checkout@v3

      - name: BKK adatok letöltése
        run: |
          curl -L "https://kozut.bkkinfo.hu/api/changes" -o adatok.json

      - name: Mentés a repository-ba
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add adatok.json
          git commit -m "BKK adatok frissítése" || exit 0
          git push
